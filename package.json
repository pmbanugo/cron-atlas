{
  "name": "cron-atlas",
  "private": true,
  "version": "1.0.0",
  "description": "a reliable serverless job scheduler",
  "scripts": {
    "start:temporal-server": "temporal server start-dev --db-filename temporal.db",
    "deploy:console": "fly deploy --remote-only --config apps/console/fly.toml --dockerfile apps/console/Dockerfile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Peter Mbanugo",
  "license": "AGPL-3.0"
}
